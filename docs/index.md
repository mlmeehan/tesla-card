# tesla-card Documentation Index

**Part:** `[card]` · **Path:** `tesla-card/` (separate nested git repo) · **Last Updated:** 2026-06-20
**Type:** Frontend UI component library — custom Lovelace card (HACS plugin)
**Primary Language:** TypeScript (strict) · **Framework:** Lit 3

> Part of a multi-part repository. For the repo-wide entry point and the `[HA]` config-as-code side,
> see the master index: [`../../docs/index.md`](../../docs/index.md).

---

## Overview

`tesla-card` renders a single Tesla (default: "Garage Model Y") as a Tesla-app-style Lovelace card:
a centered living hero with a tab bar over feature panels (charging, climate, closures, tyres,
location, media, and — when an energy site is detected — a live **Energy** flow panel), plus
quick-action toggles and command buttons. The same bundle also ships standalone **ecosystem cards**
(solar / powerwall / grid / home / wall-connector) and a composed **"My Home" energy Scene**. It
bundles to a single ES module (`dist/tesla-card.js`) consumed by Home Assistant and distributed via
HACS, organized as a `data/ ← flow/ ← components/` layered architecture. Its core design features are
**entity resolution by stable function-name** (works across installs and prefixes without hard-coded
IDs) and a **single energy sign-convention** that drives both the Hero overlay and the Scene bus.

## Quick Reference

- **Tech Stack:** TypeScript `^5.7.3`, Lit `^3.2.1`, `@mdi/js` `^7.4.47`, Rollup `^4.30.1` + terser
- **Entry Point:** `src/tesla-card.ts` → `dist/tesla-card.js`
- **Architecture Pattern:** single `@customElement('tesla-card')` orchestrating flat `tc-*` children (shadow-DOM component tree)
- **Build:** `npm run build` (Rollup → single inlined ES bundle)
- **Gates:** `npm run typecheck` + `npm run test` (Vitest unit suite) + `npm run lint` (6-gate chain) + `npm run build` + Playwright E2E + demo harness — see [CI Pipeline](./ci.md)
- **Min HA:** `2024.4.0` · **CI runtime:** Node 20
- **Tests:** Vitest unit suite — **59 files / 1171 tests** (+ Playwright e2e/visual)
- **Version:** `0.2.0` — `CARD_VERSION` in `src/const.ts`, kept in sync with `package.json` + git tag (**CI-enforced** by the `version-sync` lint gate + a `release.yml` tag assertion)

## Generated Documentation

- [Architecture](./architecture.md) — component tree, entity resolution, design tokens, Lovelace contract, build pipeline
- [Component & Primitive Inventory](./component-inventory.md) — every `tc-*` element, `ui.ts` primitive, `helpers.ts` function, and `--tc-*` token
- [Development Guide](./development-guide.md) — commands, demo harness, adding a component, CI, release/version-sync
- [CI Pipeline](./ci.md) — workflows (validate / test / release), quality gates, local-parity scripts, secrets, troubleshooting
- [Source Tree Analysis](./source-tree-analysis.md) — annotated `src/` and project layout
- [Privacy](./privacy.md) — no-network-egress / no-telemetry affirmation, the merge-blocking gate, and the sanctioned HA channel
- [Layer contract (`@unstable`)](./layer-contract.md) — the published body-render contract: named layers/nodes, registration, the 3/4 camera, the 1024×687 anchor, and the one-way-door freeze warning
- [Recolorable body](./recolorable-body.md) — how to bake the four layers the Layer contract composites (bring-your-own render)
- [Asset packs (`@unstable`)](./asset-packs.md) — bring-your-own render + multi-model packs: WebP externalization, HA placement, swapping models by URL, the never-committed boundary

## Existing Project Docs

- [`../README.md`](../README.md) — user-facing: features, install, options table, entity resolution
- [`../PUBLISHING.md`](../PUBLISHING.md) — HACS extraction + release / version-sync checklist

## Getting Started

```bash
cd tesla-card
npm ci
npm run typecheck   # strict type-check (must pass)
npm run build       # emits dist/tesla-card.js (must succeed)
# open demo/index.html in a browser to verify visually (no HA needed)
# try ?scenario=asleep / ?env=renamed / ?panel=charging
```

## For AI-Assisted Development

When extending the card, **read [`architecture.md`](./architecture.md) first**, then keep these
invariants (full list in architecture §10):

- **No hard-coded Tesla entity IDs** — resolve by stable function-name via `config.entities[key]`.
- **`useDefineForClassFields: false`** is load-bearing for Lit — never "fix" it.
- **Use `var(--tc-…)` tokens**, don't redefine them or hard-code palette values.
- **Keep the Lovelace contract** (`setConfig`/`getCardSize`/`getStubConfig`/`getConfigElement`).
- **Never commit `dist/`**; keep the bundle dependency-free beyond `lit` + `@mdi/js`.
- This is a **separate nested git repo** — commit card changes inside `tesla-card/`.

---

_Documentation generated by the BMAD Method `document-project` workflow. Architecture / component
inventory / source-tree regenerated by a 2026-06-20 deep scan to reflect Epics 3–8 (recolorable hero,
`flow/` energy engine, control/panel surface, ecosystem suite + "My Home" Scene, and the Epic-8 depth
pass: inline history charts, per-node hero art, Powerwall controls, Vehicle node + self-powered ribbon)._
